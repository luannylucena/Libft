#include "libft.h"

t_node *ft_new_stack(void *content) //pq aqui é void? 
{
    t_node *new; //declaro uma variável new.

    new = (t_node*)(ft_calloc(1, sizeof(t_node)); //alocação de memória para new
    if(!new)



}